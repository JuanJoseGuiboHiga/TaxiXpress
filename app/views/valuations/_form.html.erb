<%= simple_form_for @valuation, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :description_valuation ,label:"Descripción" %>
  <%= error_span(@valuation[:description_valuation]) %>

  <%= f.button :submit,"Registrar Feedback", :class => 'btn-primary' %>
  <%= link_to t('.cancel', :default => t("Atras")),
                pagina_principal_administrador_index_path, :class => 'btn btn-danger' %>
<% end %>
