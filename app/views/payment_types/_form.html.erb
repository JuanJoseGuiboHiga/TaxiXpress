<div class="">
<%= simple_form_for @payment_type, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :description, label:"Descripción"%>
  <%= error_span(@payment_type[:description]) %>

  <%= f.button :submit,"Registrar Tipo de Pago", :class => 'btn-primary' %>
  <%= link_to t('.cancel', :default => t("Atras")),
                pagina_principal_administrador_index_path, :class => 'btn btn-danger' %>
<% end %>
</div>